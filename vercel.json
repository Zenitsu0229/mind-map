{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "installCommand": "npm install --include=dev && chmod -R +x node_modules/.bin",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
